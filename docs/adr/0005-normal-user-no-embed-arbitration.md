# Normal user only; no multi-embedder arbitration

Win Mon runs as a normal (non-elevated) user. It does not require administrator rights for NIC sampling or tray/taskbar UI.

If another app also embeds chrome into the taskbar (for example, TrafficMonitor), Win Mon does not detect, coordinate, or resolve overlap. Clash is an accepted product limitation.
